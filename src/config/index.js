// This contains all the configuration related to currency, exchange rate, number formatting.
// TODO: Refactor currency.js and move the currency's decimal point configuration also here.
export default {
  currencies: {
    BRL: {
      hasExchangeRateInversionEnabled: true,
    },
    INR: {
      hasExchangeRateInversionEnabled: true,
    },
    JPY: {
      hasExchangeRateInversionEnabled: true,
    },
  },
};
